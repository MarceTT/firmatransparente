{
  "name": "firmatransparente",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:push": "pnpm --filter @ft/prisma db:push",
    "db:migrate": "pnpm --filter @ft/prisma migrate:dev",
    "db:seed": "pnpm --filter @ft/prisma db:seed",
    "db:studio": "pnpm --filter @ft/prisma studio"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
